<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>REFIT MASTER</title>

<style>

*{
margin:0;
padding:0;
box-sizing:border-box;
font-family:Arial,sans-serif;
}

body{
background:#f5f7fa;
}

.header{
background:#0B5394;
color:white;
padding:15px;
text-align:center;
font-size:28px;
font-weight:bold;
}

.nav{
display:grid;
grid-template-columns:repeat(2,1fr);
gap:10px;
padding:15px;
}

.nav button{
padding:15px;
font-size:16px;
border:none;
border-radius:10px;
background:#0B5394;
color:white;
}

.page{
padding:15px;
display:none;
}

.active{
display:block;
}

.card{
background:white;
padding:15px;
margin-bottom:15px;
border-radius:10px;
box-shadow:0 2px 6px rgba(0,0,0,0.15);
}

input,
textarea,
select{
width:100%;
padding:10px;
margin-top:5px;
margin-bottom:10px;
border:1px solid #ccc;
border-radius:8px;
}

.btn{
width:100%;
padding:12px;
background:#0B5394;
color:white;
border:none;
border-radius:8px;
}

table{
width:100%;
border-collapse:collapse;
}

table th,
table td{
border:1px solid #ddd;
padding:8px;
font-size:14px;
}

.total{
font-size:24px;
font-weight:bold;
text-align:right;
margin-top:10px;
}

</style>
</head>

<body>

<div class="header">
REFIT MASTER
</div>

<div class="nav">

<button onclick="showPage('dashboard')">Dashboard</button>

<button onclick="showPage('project')">Project</button>

<button onclick="showPage('price')">Price Master</button>

<button onclick="showPage('quote')">Quotation</button>

</div>

<!-- DASHBOARD -->

<div id="dashboard" class="page active">

<div class="card">

<h3>Total Projects</h3>

<h1 id="totalProjects">0</h1>

</div>

<div class="card">

<h3>Total Price Items</h3>

<h1 id="totalItems">0</h1>

</div>

</div>

<!-- PROJECT -->

<div id="project" class="page">

<div class="card">

<h3>Project Master</h3>

<label>Project ID</label>
<input id="projectId">

<label>Customer Name</label>
<input id="customerName">

<label>Contact</label>
<input id="contact">

<label>Address</label>
<textarea id="address"></textarea>

<label>Project Title</label>
<input id="projectTitle">

<button class="btn" onclick="saveProject()">
Save Project
</button>

</div>

</div>

<!-- PRICE MASTER -->

<div id="price" class="page">

<div class="card">

<h3>Price Master</h3>

<label>Item Code</label>
<input id="itemCode">

<label>Description</label>
<input id="itemDescription">

<label>Unit</label>
<input id="itemUnit">

<label>Rate</label>
<input id="itemRate">

<button class="btn" onclick="saveItem()">
Save Item
</button>

</div>

<div class="card">

<table id="priceTable">

<tr>
<th>Code</th>
<th>Description</th>
<th>Rate</th>
</tr>

</table>

</div>

</div>

<!-- QUOTATION -->

<div id="quote" class="page">

<div class="card">

<h3>Quotation</h3>

<label>Select Item</label>

<select id="quoteItem"
onchange="loadItem()">

<option value="">
Select Item
</option>

</select>

<label>Description</label>
<input id="quoteDescription" readonly>

<label>Unit</label>
<input id="quoteUnit" readonly>

<label>Rate</label>
<input id="quoteRate" readonly>

<label>Qty</label>
<input id="quoteQty"
onkeyup="calculateAmount()">

<label>Amount</label>
<input id="quoteAmount" readonly>

<button class="btn"
onclick="addQuoteItem()">
Add Item
</button>

</div>

<div class="card">

<table id="quoteTable">

<tr>
<th>Description</th>
<th>Qty</th>
<th>Rate</th>
<th>Amount</th>
</tr>

</table>

<div class="total">
RM <span id="grandTotal">0.00</span>
</div>

</div>

</div>

<script>

let projects =
JSON.parse(localStorage.getItem("projects")) || [];

let items =
JSON.parse(localStorage.getItem("items")) || [];

let quoteTotal = 0;

function showPage(page){

document.querySelectorAll(".page")
.forEach(p=>p.classList.remove("active"));

document.getElementById(page)
.classList.add("active");

}

function saveProject(){

let data = {

projectId:
document.getElementById("projectId").value,

customer:
document.getElementById("customerName").value,

contact:
document.getElementById("contact").value,

address:
document.getElementById("address").value,

title:
document.getElementById("projectTitle").value

};

projects.push(data);

localStorage.setItem(
"projects",
JSON.stringify(projects)
);

document.getElementById(
"totalProjects"
).innerText=projects.length;

alert("Project Saved");

}

function saveItem(){

let item = {

code:
document.getElementById("itemCode").value,

description:
document.getElementById("itemDescription").value,

unit:
document.getElementById("itemUnit").value,

rate:
document.getElementById("itemRate").value

};

items.push(item);

localStorage.setItem(
"items",
JSON.stringify(items)
);

loadPriceTable();
loadDropdown();

alert("Item Saved");

}

function loadPriceTable(){

let table =
document.getElementById("priceTable");

table.innerHTML = `
<tr>
<th>Code</th>
<th>Description</th>
<th>Rate</th>
</tr>
`;

items.forEach(i=>{

table.innerHTML += `
<tr>
<td>${i.code}</td>
<td>${i.description}</td>
<td>${i.rate}</td>
</tr>
`;

});

document.getElementById(
"totalItems"
).innerText = items.length;

}

function loadDropdown(){

let select =
document.getElementById("quoteItem");

select.innerHTML =
'<option value="">Select Item</option>';

items.forEach((i,index)=>{

select.innerHTML +=
`<option value="${index}">
${i.code}
</option>`;

});

}

function loadItem(){

let index =
document.getElementById("quoteItem").value;

if(index==="") return;

let item = items[index];

document.getElementById(
"quoteDescription"
).value=item.description;

document.getElementById(
"quoteUnit"
).value=item.unit;

document.getElementById(
"quoteRate"
).value=item.rate;

}

function calculateAmount(){

let qty =
Number(document.getElementById(
"quoteQty"
).value);

let rate =
Number(document.getElementById(
"quoteRate"
).value);

document.getElementById(
"quoteAmount"
).value =
(qty*rate).toFixed(2);

}

function addQuoteItem(){

let desc =
document.getElementById(
"quoteDescription"
).value;

let qty =
document.getElementById(
"quoteQty"
).value;

let rate =
document.getElementById(
"quoteRate"
).value;

let amount =
document.getElementById(
"quoteAmount"
).value;

document.getElementById(
"quoteTable"
).innerHTML += `
<tr>
<td>${desc}</td>
<td>${qty}</td>
<td>${rate}</td>
<td>${amount}</td>
</tr>
`;

quoteTotal += Number(amount);

document.getElementById(
"grandTotal"
).innerText =
quoteTotal.toFixed(2);

}

loadPriceTable();
loadDropdown();

document.getElementById(
"totalProjects"
).innerText =
projects.length;

</script>

</body>
</html>